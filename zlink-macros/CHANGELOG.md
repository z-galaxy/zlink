# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## 0.7.0 - 2026-07-19

### Added
- ✨ Support skip and flatten in Type/CustomType.
- ✨ Support `rename_all_arguments` on the proxy macro. #170

### Changed
- 🏗️ Make the runtime features additive.
- 🚚 Support renaming ReplyError variants.
- 🎨 Collapse nested ifs into let-chains.

### Dependencies
- ➕ Depend on zlink-idl for name validation.

### Documentation
- 📝 Mention the book in the README.
- 📝 Document the rename attributes.
- 📝 Link to the examples directory instead of duplicating it.
- 📝 Remove obsolete USB transport note and document smol crate.

### Fixed
- 🚑️ Unraw idents in the service macro. #292
- 🚑️ Unraw idents when deriving names. #290
- 🩹 Don't abort attr parsing on valueless keys.

### Other
- 🦺 Validate emitted names against the Varlink grammar. #296
- 💣 Unraw idents in the proxy macro.
- 🚨 Allow non-camel-case variants in ReplyError's helper. #293
- 🏷️ Honor renaming in Type and CustomType derives.

### Testing
- ✅ Use valid Varlink names in the tests and doc examples.

## 0.6.0 - 2026-07-11

### Added
- ✨ Support custom types per interface in introspection.

### Breaking
- 💥 Require rename for underscore-prefixed method params. #278

### Changed
- 🔧 Replace hardcoded /tmp socket paths with tempfile.

### Documentation
- 💡 Fix stale comment about server task in tests.
- 📝 Add doc-comments to BankAccount test service.

### Fixed
- 🐛 Fix introspection to include method & interface doc-comments.

### Other
- 🚨 Fix clippy warnings from latest stable Rust.

### Testing
- ✅ Add test for underscore-prefixed method params.
- ✅ Add tests for method & interface doc-comment introspection.

## 0.5.0 - 2026-05-03

### Added
- ✨ Detect `Result<Reply<T>, E>` items in streaming methods.
- ✨ Allow fallible reply streams in `Service`.

### Changed
- 🎨 Apply nightly rustfmt to streaming docs.

### Documentation
- 📝 Document streaming-method error replies on `service`.
- 📝 Frame streaming-error gap as a temporary limitation.
- 📝 Document streaming `more` methods on `service` macro.

### Fixed
- 🚑️ generate service method out params.
- 🐛 Make sure methods return objects in tests.

### Testing
- ✅ Test method in/out params in introspection.
- ✅ Round-trip test for streaming method errors.

## 0.4.2 - 2026-04-26

### Documentation
- 📝 Configure docs.rs to build for all supported targets.

## 0.4.1 - 2026-03-28

### Added
- 🚸 Allow expressions for values of `service` attributes. #237

### Dependencies
- ⬆️  Upgrade to Rust edition 2024.

### Fixed
- 🚑️ Add `+ 'static` on `impl Stream` return.
- 🐛 Allow explicit lifetimes in service method args. #231
- 🚑️ Don't assume futures_util dep. #230

## 0.4.0 - 2026-02-22

### Added
- ✨ `service` now allows returning borrowed errors.
- ✨ Support FDs in Service impls.
- ✨ `service` methods can now return reply streams. #211
- ✨ Allow `interface` attribute on `service`.
- ✨ Implement introspection interface. #213
- ✨ Add `service` attribute macro. #76

### Breaking
- 💥 Move `Sock` from `handle` to the trait in Service. #207

### Changed
- ♻️ Split `service` tests into separate files.
- 🚚 Move `service` tests from zlink to -macros.

### Documentation
- 📝 Document `service` feature.
- 📝 Use & recommend `service` macro.
- 💡 Add missing `.` in sample code comments.

### Fixed
- 🐛 Remove `_` prefix from names for introspection.
- 🐛 Hide internal types generated by `service`.
- 🚑️ Fix a borrowing problem in service methods. #216

### Other
- 🚩 Reject FD attributes in service macro without std.
- 🚩 `service` feature now requires `introspection` feature.
- ⏪️ Revert "🚩 macros: Enable needed features on zlink dev-dep".
- 🚩 Enable needed features on zlink dev-dep.

## 0.3.0 - 2026-01-12

### Breaking
- 💥 chain::ReplyStream's items now owned. #185

### Changed
- 🏗️ Move reply types from Chain to send method.
- ♻️ Rename service handle lifetime to 'service.

### Documentation
- 📝 Document `oneway` attribute of `proxy` methods.
- 📝 List forgotten `smol` feature.
- 📝 Drop now incorrect `no_std` claims.

### Fixed
- 🐛 macros shouldn't set features on zlink.

### Other
- 🦺 Box the Future type of the ReplyStream.
- `proxy` to require owned return types from streaming methods.
- ✏️ Fix a typo.
- ✏️  Add missing `.` at the end of sentences.

### Performance
- ⚡️ `proxy` generates chain methods for oneway methods.
- ⚡️ Allow service to return borrowed data from call.
- ⚡️ Re-use boxed allocations.

### Removed
- 🔥 `proxy` only generate chain methods for owned types ret.

### Testing
- ✅ Drop an unused import.
